# Core ML/Transformers
transformers>=4.40.0
accelerate>=0.30.0
torch>=2.2.0
sentencepiece>=0.1.99  # Required for LLaMA tokenizer
scipy>=1.11.0
numpy>=1.24.0
einops>=0.7.0  # For tensor manipulations in steering

# Vector steering specific
scikit-learn>=1.3.0  # For PCA or clustering methods
matplotlib>=3.7.0    # For visualizations of directions or activations
plotly>=5.15.0       # Interactive visualizations
pandas>=2.0.0        # Data manipulation

# Jupyter + VS Code integration
notebook>=7.0.0
ipykernel>=6.25.0
jupyterlab>=4.0.0

# File handling / utils
tqdm>=4.66.0
rich>=13.4.0
datasets>=2.18.0  # if using HF datasets
seaborn>=0.12.2   # for visualizing activations/directions (optional)

# Optional: visualization for internal representations
# umap-learn>=0.5.6  # Updated for Python 3.12 compatibility - DISABLED due to numba dependency issues

# Optional: tokenizers (custom or fast)
tokenizers>=0.19.0

# For CLI-style experimentation
ipython>=8.15.0

# Additional utilities for steering experiments
jaxtyping>=0.2.20    # Type hints for tensor shapes
typeguard>=4.0.0     # Runtime type checking
wandb>=0.16.0        # Experiment tracking (optional) - updated for Python 3.12
safetensors>=0.4.0   # Safe tensor serialization - updated
